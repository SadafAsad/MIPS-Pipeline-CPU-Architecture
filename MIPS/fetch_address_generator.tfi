


// Instantiate the module
fetch_address_generator instance_name (
    .hit(hit), 
    .PCSrc(PCSrc), 
    .address_branch(address_branch), 
    .address_in(address_in), 
    .instruction_address(instruction_address), 
    .address_out(address_out)
    );


